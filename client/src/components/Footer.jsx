import React from "react";
import Logo from "../img/logo.png";

const Footer = () => {
  return (
    <footer>
      <img src={Logo} alt=".blog" />
      <span>Create with React, Node and MySQL</span>
    </footer>
  );
};

export default Footer;
